# Все значения необязательны: без них работает детерминированный demo-режим.
PORT=3000
DB_PATH=data/ams.db
PLANNER=mock            # mock | llm
# Нужны только для PLANNER=llm (OpenAI-совместимый API). Не коммитьте реальные ключи.
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=
