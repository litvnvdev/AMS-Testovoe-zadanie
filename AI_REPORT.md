# AI-report

**Фактически затраченное время:** ____ ч (заполните своим временем; лимит задания 4 часа).
**Использованные AI-инструменты:** Claude (Cowork) для проектирования и генерации кода; ___ (допишите, если пользовались другими).

## Что сгенерировано AI

- Архитектура и разбиение на модули (API, TaskService, Planner, Gateway, Policy, Storage, RecordsSystem).
- Весь код сервера на TypeScript, React-панель, три теста, Dockerfile и compose, README.

## Где AI ошибся или что пришлось исправить

Заполните по факту после запуска у себя. Что уже известно:

- Первый вариант зависимостей включал `@tanstack/react-query`; от него отказались в пользу собственного хука опроса (`web/src/usePolling.ts`), чтобы уменьшить число зависимостей.
- Код и тесты писались **без запуска** (в среде генерации был закрыт доступ к npm). Синтаксис `.ts` файлов проверен парсером Node, но `tsc`, `vitest`, сборка Vite и Docker в той среде не выполнялись.
  Все найденные при первом запуске ошибки типов и падения тестов записывайте сюда: это и есть честный список «где AI ошибся».

## Что проверено вручную (отметьте после проверки)

- [ ] `npm install && npm test`: три теста проходят.
- [ ] `npm run typecheck` без ошибок.
- [ ] `docker compose up --build` с чистого окружения, страница открывается.
- [ ] Демо по README: подтверждение, повторное подтверждение (`409`), повтор `task_id` (`duplicate`), запись в `read_only`, неизвестный инструмент.
- [ ] Перезапуск контейнера: задача и события на месте.
- [ ] В журнале аудита нет секретов и полного ввода (только `input_hash`).
